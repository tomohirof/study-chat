<svg width="512" height="512" viewBox="0 0 512 512" xmlns="http://www.w3.org/2000/svg">
  <!-- Background (black for PWA) -->
  <rect width="512" height="512" fill="#000000"/>

  <!-- Large C letter (white) -->
  <text
    x="256"
    y="380"
    font-family="Arial, sans-serif"
    font-size="400"
    font-weight="bold"
    fill="#ffffff"
    text-anchor="middle"
  >C</text>
</svg>
